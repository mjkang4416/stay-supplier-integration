# JOURNAL

## 설계 의사결정 기록

<!--
결정마다 아래 형식으로 적어요. 결론만 README 4장에 옮겨요.
### 결정 이름
#### 문제
#### 선택지 비교
| 기준 | 선택지 1 | 선택지 2 |
|---|---|---|
#### 예시 데이터로 따져 보기
#### 결정과 이유
#### 폐기한 대안과 이유
#### 잃는 것
#### 다시 검토할 조건
-->

### 표준 숙박 상품 모델: 살린 정보와 버린 정보

### 요금 기준

### 재고 표현

### 정적·동적 데이터 분리
#### 문제
공급사 API는 성격이 다른 두 단계로 나뉘어요. 숙소 목록(①)은 조건 없이 전체를 주고 자주 바뀌지 않아요. 재고·요금(②)은 날짜·인원을 받아야 하고 호출할 때마다 달라질 수 있어요. 이 차이를 저장·호출·실패 처리에 어떻게 반영할지 정해야 해요.
#### 조사
도메인 리서치 4장. 요금은 시간 단위(자동 RMS 기준 하루 12\~40회), 재고는 예약·취소 이벤트마다, 숙소 정보는 일·주 단위로 바뀌어요. 업계는 정적 콘텐츠를 야간·주간 배치로 로컬 DB에 두고 동적 데이터는 검색마다 실시간 호출해요.
#### 결정과 이유
| | 정적: 숙소·객실 타입 | 동적: 재고·요금 |
|---|---|---|
| 저장 | DB 매핑 테이블 | 저장 안 함 |
| 호출 | 하루 1회 갱신 잡 + 기동 시 + 수동 | 검색 요청마다 |
| 실패 | 기존 매핑 유지, 앱은 정상 기동 | 타임아웃 뒤 부분 실패로 응답 |
| 읽기 | 앱이 기동 시 DB → 인메모리, 검색은 인메모리에서 | 어댑터 응답을 바로 사용 |
쓰는 쪽(갱신 잡)과 읽는 쪽(앱 서버)을 분리해요. 이번 범위는 단일 프로세스라 갱신 잡을 앱 안에서 `@Scheduled`로 돌리되 별도 서비스 클래스로 두고, 다중 인스턴스 환경에서는 별도 잡 프로세스(CronJob)로 떼어내고 각 인스턴스는 갱신 시각을 확인해 리로드하는 설계로 남겨요.
#### 폐기한 대안과 이유
- 검색할 때마다 숙소 목록도 호출: 자주 안 바뀌는 전체 목록을 매번 받는 낭비이고, 매핑을 저장하지 않으면 식별자 안정성을 보장할 수 없어요.
- 재고·요금을 DB에 저장: 이벤트마다 바뀌는 값을 저장하면 항상 옛 값이 돼요. 요구사항도 원본을 쌓아두지 말라고 해요.
#### 잃는 것
갱신 잡이 돈 뒤 다음 갱신까지 새로 추가된 숙소는 검색에 안 나와요(최대 하루). 수동 갱신으로 보완해요.

### 매핑 생성 시점과 실패 처리
#### 문제
숙소 목록 API를 언제 호출해 매핑을 만들지, 호출이 실패하면 어떻게 할지.
#### 선택지 비교
| 기준 | 앱 기동 시만 | 주기 스케줄만 | 수동 명령만 | 기동 시 + 매일 1회 + 수동 |
|---|---|---|---|---|
| 첫 검색 전 매핑 보장 | O | X (첫 실행 전 비어 있음) | X | O |
| 운영 중 변경 반영 | X (재기동 필요) | O | 사람이 해야 함 | O |
| 구현 비용 | 낮음 | 낮음 (`@Scheduled`) | 낮음 (엔드포인트) | 낮음 |
| 다중 인스턴스 | 인스턴스마다 호출 | 한 인스턴스만 실행하도록 락 필요 | - | 잡을 분리하면 해결 |
#### 결정과 이유
(처음) 기동 시 + 매일 새벽 1회 + 수동 엔드포인트로 정했다가, (변경) 갱신 잡을 K8s CronJob 별도 팟(`sync` 프로필)으로 분리하고 수동 엔드포인트는 없앴어요. 웹 앱은 기동 시 DB에서 동기 로드하고 매일 04:30에 한 번 더 읽어 교체하며 공급사 숙소 목록 API를 직접 호출하지 않아요. 중간에 10분 주기 리로드를 넣었다가, 매핑이 하루 1회만 바뀌므로 인메모리도 하루 1회면 충분하다는 지적으로 뺐다. 처음 안은 숙소 정보가 일·주 단위로 바뀐다는 조사 결과에 맞춰 하루 1회로 두고, 첫 검색 전 매핑이 있어야 하므로 기동 시에도 실행하며, 급한 변경은 수동으로 반영해요. 실패하면 해당 공급사만 건너뛰고 기존 매핑을 유지한 채 기동해요. 공급사 하나 때문에 서비스 전체가 못 뜨면 안 되기 때문이에요.
#### 폐기한 대안과 이유
- 기동 시만: 운영 중 숙소가 추가되면 재기동 전까지 검색에 안 나와요.
- 검색할 때마다: 정적·동적 분리 항목 참고.
- 실패 시 기동 중단: 외부 연동은 실패할 수 있다는 전제(P6)와 어긋나요.
- 웹 앱 안에서 `@Scheduled`로 갱신: 인스턴스가 여러 대면 한 인스턴스만 돌게 락이 필요하고, 웹 앱이 공급사 호출 실패의 영향을 받아요. CronJob으로 떼어내면 둘 다 사라지고 갱신 잡을 독립적으로 실행·재실행할 수 있다(동료 저장소의 runner 패턴과 같음).
- 수동 갱신 엔드포인트: 운영 API 표면이 늘고 인증 문제가 생겨요. CronJob 즉시 실행으로 대체.
- 사라진 숙소 판정을 `last_seen_at` 마커로: 컬럼을 하나 더 두고 갱신 시각을 찍는 방식을 검토했으나, 이번 목록과 DB의 active 집합을 직접 비교하는 델타 계산이 추가·사라짐·변경을 코드에 그대로 드러내고 캐시 제거와도 이어져서 이쪽을 택했어요. 컬럼도 하나 줄어들어요.
#### 잃는 것
새벽 갱신 사이에 생긴 변경은 최대 하루 늦게 반영돼요. 목록에서 사라진 숙소는 삭제하지 않고 비활성으로만 표시하므로 테이블이 줄지 않아요.
#### 갱신 시각
"새벽"이 아니라 "소비자 접근이 가장 적은 시간대"가 기준이에요. 국내 숙박 앱의 시간대별 이용률은 공개 자료가 없어요. 호텔 예약 피크(저녁 9시, 예약 완료 기준·해외)와 일반 웹 트래픽 최저(03\~05시, 모든 사이트 합산)는 숙소 검색을 잰 값이 아니라 가정의 근거일 뿐이므로, 한국 시간(Asia/Seoul) 03\~05시 창 안에서 기본값 04:00을 설정값으로 두고, 운영에서 검색 요청 지표로 시간대별 검색량을 직접 재서 정해요. DB와 컨테이너가 UTC라 스케줄에 타임존을 명시해요. 인메모리 교체와 델타 write라 sync가 사용자 응답에 주는 영향은 거의 없고, 시간대를 고르는 이유는 공급사 호출 한도을 검색 피크와 겹치지 않게 하려는 것이에요.
#### 다시 검토할 조건
공급사가 변경 알림(푸시)을 제공하면 주기 갱신을 이벤트 기반으로 바꿔요. 인스턴스가 여러 개가 되면 갱신 잡을 별도 프로세스로 분리하고 리로드 방식(주기 리로드 / 갱신 시각 확인 / pub/sub 알림)을 정해요.

### 내부 식별자 안정성
#### 문제
같은 공급사 상품이 다시 조회되면 항상 같은 내부 식별자가 나와야 해요. 재실행·동시 실행·숙소 삭제 상황에서도 지켜져야 해요.
#### 선택지 비교
| 기준 | BIGINT 자동 증가 + 유니크 키 | UUID/ULID | 공급사 코드 해시 |
|---|---|---|---|
| 같은 코드 → 같은 식별자 | 유니크 키 + upsert로 보장 | 유니크 키 + upsert로 보장 | 해시 자체로 보장 |
| DB를 새로 만들면 | 번호가 달라질 수 있음 | 값이 달라짐 | 같음 |
| 외부 노출 | 순번이 드러남 | 추측 불가 | 코드가 드러남 |
| 인덱스·조인 | 가장 가벼움 | 큼 | 중간 |
#### 결정과 이유
BIGINT 자동 증가 + `(supplier, supplier_hotel_code)` 유니크 키 + `INSERT … ON DUPLICATE KEY UPDATE`. 객실 타입은 `(hotel_id, supplier_room_type_code)` 유니크 키. 행은 절대 삭제하지 않고 목록에서 사라지면 `active=false`로 표시해요. 동시 실행은 유니크 키가 막고, upsert 한 문장이라 경합에서도 같은 행을 가리킨다.
#### 폐기한 대안과 이유
- 삭제 후 재삽입 방식 갱신: 사라졌다가 다시 나타난 숙소가 새 식별자를 받아요.
- 코드 해시: 재현성은 좋지만 충돌 처리와 길이 문제가 있고, 유니크 키만으로 같은 효과를 얻어요.
#### 잃는 것
DB를 초기화하면 식별자가 달라져요. 운영에서는 DB를 초기화하지 않는 것이 전제예요.

### 요금/재고 캐시 (선택 구현, 설계만)
#### 문제
검색마다 ②를 호출하면 인스턴스 수와 검색량에 비례해 공급사 호출이 늘고 호출 한도(429)에 걸릴 수 있어요. 캐시를 두면 옛 값을 보여줄 위험이 생겨요.
#### 검토 과정
1. 서버 시작 시 한 번 캐싱: 재고가 이벤트마다 바뀌어 곧 틀려요. 폐기.
2. 1시간마다 전체 폴링: ②는 날짜·인원이 필요해 전체 조합을 미리 긁을 수 없어요. 스냅샷 파일을 따로 주는 공급사에서만 가능한 방식. 폐기.
3. 인스턴스별 인메모리 캐시: 인스턴스마다 값이 달라지고 호출이 인스턴스 수만큼 중복돼요. 호출 한도 때문에 공유 캐시(Redis)가 필요하다고 판단.
4. "주기 갱신이면 TTL이 필요 없다": 갱신 주기가 곧 허용 오차이고, 어떤 키를 갱신 목록에 넣고 뺄지 규칙이 없으면 검색된 적 있는 모든 조합을 영원히 갱신하게 돼요. 결국 만료 규칙이 필요해요.
5. 전체 미리 채우기의 갱신 주기는 호출 한도으로 정해진다: 갱신 한 번의 호출 수 = ⌈숙소 수/50⌉ × 날짜 범위 × 인원 조합. 숙소 1,000개·365일·초당 10회면 최소 12분. 30일 범위이면 1분.
6. Supplier B는 기간 총액만 주므로 날짜별 캐시로는 연박 요금을 재구성할 수 없어요. 키는 날짜 범위 단위여야 해요.
7. "Redis에 몇 개씩 넣을지": 병목은 Redis 쓰기가 아니라 공급사 호출 한도예요. 웜 채우기 처리량은 백그라운드 호출 한도(허용 속도 × 배분 비율)으로 정해지고, 토큰 버킷으로 고르게 호출해요. A는 긴 기간 한 번 호출로 여러 날을 채울 수 있지만 B는 검색 단위마다 호출해야 해서 비용이 커요.
8. 숙소가 수천 개로 늘면 30일 전체를 분 단위로 돌릴 수 없어요. 날짜 거리별로 계층을 나눠(D-7까지 5분, D-30까지 30분\~1시간, 그 너머는 콜드) 예산을 배분해요. 하루 단위 갱신은 매핑(정적)에만 해당하고, 요금·재고 웜 캐시를 하루 단위로 돌리면 시간 단위로 변하는 요금과 맞지 않아요.
9. 공급사 초당 호출 한도는 스펙에 없어요(요청당 50개 상한과 429의 존재만 명시). 허용 속도를 설정값으로 두고 보수적으로 시작해 429 관측으로 자동 감속·증속(AIMD)하는 방식으로 설계해요. 산식의 "초당 10회"는 계산 예시일 뿐이에요.
10. "공급사가 여러 판매처에 팔면 재고 변경이 검색보다 잦지 않은가": Look-to-Book 비율(도매 업체 집계 검색 1,400만/예약 8만 = 약 175:1, 유통사 수준 수천:1)로 보면 공급사 안에서 집계해도 재고 변경이 검색보다 두 자릿수 이상 드물어요. 캐시는 인기 키에서 성립하고, 마지막 방(1→0)은 예약 직전 재확인으로 방어해요. 변동성이 높은 숙소는 불일치율로 골라 TTL을 줄이거나 우회해요.
11. 출처 검증: Look-to-Book 수치는 공식 통계가 아니에요. 도매 업체 처리량은 제3자 연동 업체 페이지 인용(공식 페이지에서 미확인), 20,000:1은 2016년 경험 기반 칼럼, 상한 1,000\~5,000:1은 근거 미표기 블로그. 독립된 세 출처가 같은 규모를 가리키는 점만 근거로 삼고, 문서에 출처 성격을 명시했어요.
12. 캐시 주기는 변동이 잦은 항목이 정해요. 재고 변경 빈도를 공식 통계(STR 점유율 62%, SiteMinder 숙박일수 1.61박·리드타임 31일)로 역산하면 200실 호텔의 (호텔, 날짜) 키는 약 5시간, 임박 키는 약 1.6시간에 1건. 요금은 RMS 기준 30분\~2시간. 둘 중 짧은 요금 쪽(30분)을 출발값 근거로 삼아요.
13. 모델 정리: 요청 시 채우고 TTL로 만료하는 방식과 주기 갱신 방식을 섞어 설명해 혼란이 있었어요. 주기 갱신형 저장소로 통일해요. Redis에 표준 모델을 올려 두고 갱신 잡이 계층별 주기(D-3까지 5분, D-14까지 15분, D-30까지 60분)로 값을 갱신하며, 매일 새벽 매핑 델타를 반영해요. TTL은 만료 장치가 아니라 갱신 잡이 멈췄을 때를 위한 안전장치(주기의 3배)다.
14. 근거의 한계: RMS 갱신 횟수는 벤더 제품 사양(대리 지표)이고 재고 변경 빈도는 공식 통계 역산이에요. 공급사 응답 값이 실제로 얼마나 자주 바뀌는지 잰 공개 데이터는 없어요. 그래서 갱신 잡이 직전 값과 비교해 변경 감지율을 기록하고, 5% 미만이면 주기를 늘리고 30% 초과면 줄이는 규칙으로 실측 기반 조정을 설계에 넣었어요.
15. "재고가 가격보다 잦지 않은가, 재고 소프트웨어의 최댓값을 재면": PMS API의 property당 초당 5회(Cloudbeds 공식) 같은 기술 상한은 실제 예약 건수보다 세 자릿수 위라 근거가 못 돼요. 호텔 단위로는 재고가 잦지만(15분에 1건), 캐시 키(객실 타입·날짜) 단위로는 재고·요금 모두 시간 단위예요. 짧은 쪽(요금 30분)에 허용 오차 약 15%를 적용해 임박 5분을 출발값으로 잡고 변경 감지율로 조정해요.
16. "키 집합 밖은 직접 호출"은 캐시 목적과 어긋난다는 지적을 받아 구조를 바꿨어요. 인원은 스펙상 필터로만 작용하므로 adults=1로 모든 객실 타입을 받아 저장하고 검색 시 maxOccupancy로 걸러요. 날짜는 Hash 필드 `{roomTypeId}:{date}`로 펼쳐 어떤 범위든 HMGET으로 조립해요. A는 30일 범위 한 번 호출로 채우고, B는 날짜마다 1박 호출로 채우되 연박 요금은 1박 합으로 근사한다(예약 직전 재확인으로 보정).
17. 최종 정리: 프런트 트래픽 절감은 우리 범위가 아니고 인스턴스별 캐시는 여러 대에서 효과가 희석되므로, 목표 구조는 관리형 Redis(예: ElastiCache) 공유 캐시로 확정. 이번 제출은 마감 일정과 필수 동작(병렬·타임아웃·부분 실패) 시연을 우선해 설계만 남기고, 도입 시점은 4.7의 429 메트릭 모니터 신호로 판단해요. 429는 초당 횟수가 아니라 HTTP 상태 코드(Too Many Requests)다.
18. 정정: Redis 도입을 429 신호에 걸지 않아요. 목표 구조에서 Redis는 상시 운영이고, 검색은 Redis만 읽으며 공급사 호출은 갱신 잡이 정해진 속도로만 내요. 429는 그 속도를 줄이는 신호일 뿐이에요. 초당 호출 횟수는 공급사당 2회에서 시작해 AIMD로 조정하고, 계층별 주기는 목표값(5/15/60분)과 예산으로 산출한 값 중 큰 쪽을 써요.
19. 확정: 초당 호출은 공급사당 1회에서 시작하고 상한은 4회예요. 실제 공급사 API가 공개한 한도(Booking.com 샌드박스 초당 0.8회, Hotelbeds 초당 4회, Amadeus 초당 10회, Expedia는 요청 부하 기준)의 아래쪽에서 출발해요. 429는 로그·지표를 남기고 지수 백오프(최대 60초, Booking.com이 429 뒤 약 1분 차단하는 것에 맞춤) 후 절반으로 재개하며, 올리는 것은 AIMD 자동 증속 대신 Datadog 알림을 본 사람이 설정값으로 해요. 이미 모니터가 있어 자동 증속 코드는 두지 않는 편이 단순해요. 갱신 주기는 5/15/60분으로 확정. 숙소 1,000개 검산은 분당 36회 필요, 갱신 잡 예산 분당 54회(초당 1회의 90%).
20. 캐시 어사이드 검토: 검색이 Redis에 없는 (숙소, 기간)만 공급사에 물어 채우는 방식을 검토했어요. 잡이 없고 검색된 조합만 채워 단순하지만, 새 기간을 처음 검색한 사용자가 묶음 수 ÷ 허용 속도(1,000개·초당 4회면 5초)를 기다려요. "검색은 ms여야 한다"는 기준에 어긋나 폐기하고 미리 채우기로 돌아갔어요. 그 과정에서 안내 문서의 기본 흐름(검색마다 호출)과 캐시가 충돌하지 않는지 확인했어요. 문서는 캐시를 선택 항목으로 열어 두었고, 직접 호출 경로는 `fresh=true`와 저하 모드에 남겨요.
21. 값 형식: `재고|요금|세금|통화|조식|기준`에서 `재고|세금 포함 1박|통화|조식`으로 줄였어요. B가 세금 금액을 주지 않아 세금 별도 기준으로는 A·B를 통일할 수 없고, 공통 기준은 고객 결제 금액뿐이에요. A의 날짜별 단가·세금 내역은 총액으로 합쳐지며 사라져요.
22. 허용 속도: 초당 1회 → 8회 → 4회. "최악의 경우(Redis 비었을 때) 1,000개를 3초 안에"를 역산하면 20묶음 ÷ (3초 − 지연 0.5초) ≈ 8회라 8회를 검토했으나, 캐시가 완전히 비는 것은 Redis 장애뿐인 예외 케이스라 공개된 운영 한도(4회)로 시작하고 5초 복구를 받아들이기로 했어요. 응답 시간 상한은 3초에서 끊어 채운 것만 응답하고 나머지는 `pending`. 검색 응답 시간 근거(3초 넘으면 이탈 53%, 메타서치의 부분 응답 관행)는 도메인 리서치에 적었어요.
23. 주기: 날짜 거리별 계층(5/15/60분)을 단일 5분으로 단순화하고 계층은 확장 설계로 남겼어요. 남은 객실 ≤ 2인 키의 1분 핫 리스트도 설계만. TTL은 주기 × 3 안전장치.
#### 결정 (구현. 계층 주기·핫 리스트·readiness 연동·리더 선출·pending 은 설계만)
- 미리 채우기. 웹 앱 안의 스케줄 잡이 5분마다 범위(오늘\~+30일) 전체를 Redis에 올리고, 검색은 Redis만 읽어요. 직접 호출은 `fresh=true`(예약 직전 재확인)와 Redis가 비었을 때의 저하 모드(응답 시간 상한 3초, 채운 것만 응답 + `pending`)뿐.
- 숙소당 Hash `stay:v1:{hotelId}`, 필드 `{roomTypeId}:{yyyyMMdd}`, 값 `재고|세금 포함 1박|통화|조식`. adults=1로 모든 객실 타입을 받아 저장하고 인원 필터는 검색 시. 공급사별 상태 키로 실패를 검색 `failures`에 드러내요.
- 허용 속도 공급사당 초당 4회 시작, 상한 10회. 429는 로그 + 지수 백오프(최대 60초) 후 절반, 올리는 것은 사람이. 주기 5분, TTL 주기 × 3. 첫 갱신 전엔 readiness를 올리지 않아요.
- 숙소 Hash는 쓸 때마다 통째로 교체해 지난 날짜 필드가 쌓이지 않게 하고, 비활성 숙소 키는 인메모리에서 빠져 TTL로 사라져요. 접두사 버전으로 교체. 원본이 아니라 정규화한 표준 모델을 캐싱해요.
- 직접 호출 경로(안내 문서 필수 흐름)를 먼저 구현하고, 갱신 잡과 Redis 읽기는 마지막에 별도 커밋으로 얹었어요. 값에는 최대 인원도 넣어 검색이 캐시에서도 ② 응답 값을 써요.
#### 잃는 것
캐시 TTL만큼 옛 값을 보여줄 수 있어요. 검색 결과를 참고값으로 두고 예약 직전 재확인으로 흡수해요.

### 어댑터 반환 형태와 실패 판정
#### 문제
A는 HTTP 상태 코드로, B는 HTTP 200 + `resultCode`로 실패를 알려요. 응답 구조도 달라요(`items[].hotelCode` / `data.items[].propertyId`). 이 차이가 크론잡·검색 코드로 새어 나가지 않게 하면서, 신규 공급사를 붙일 때 고치는 범위를 좁혀야 해요.
#### 선택지 비교
1. 서비스에서 WebClient를 직접 부르고 JSON을 Map으로 읽기: 가장 빠르지만 A·B 분기가 서비스에 퍼지고 공급사 C가 오면 서비스를 고쳐요. 폐기.
2. A·B 필드를 모두 담은 공통 DTO 하나: 절반이 항상 null이고 어느 필드가 어느 공급사 것인지 코드가 말해 주지 않아요. 폐기.
3. `switch (supplier)` 분기: 새 공급사마다 switch를 고쳐야 해요. 폐기.
4. 인터페이스 하나 + 공급사별 패키지 구현 + 전용 형식은 package-private: 채택.
5. 실패를 결과 객체(성공/실패)로 반환 vs 예외 하나로 통일: 타임아웃·5xx는 WebClient가 예외로 주므로 어댑터마다 예외→결과 변환을 반복하게 돼요. 어댑터는 예외 하나로 통일하고 호출자가 공급사별 결과로 감싸는 쪽을 택했어요. 묶음 여러 개 중 일부만 실패하는 ② 호출에서는 부분 결과가 필요하므로, ② 설계에서 `SupplierFetchResult(성공 항목, 실패 묶음)`을 추가하기로 했어요.
6. 어댑터가 저장까지 할지: 같은 어댑터를 크론잡(DB 델타)·검색(응답·캐시)·실시간 재확인이 다르게 쓰고, 검색이 DB에 쓰면 "요금·재고 저장 금지"를 어겨요. 호출과 번역까지만 어댑터, 저장은 서비스. 재시도도 호출자마다 정책이 달라(크론잡 30초×3, 검색 200ms×1) 호출자 책임.
#### 결정과 이유
- `SupplierClient` 인터페이스, `supplier.a`/`supplier.b` 구현체, 전용 응답 형식은 package-private. 밖으로 나가는 것은 `SupplierHotel`·`SupplierRoomType`·`FailureReason`뿐.
- 실패는 `FailureReason` 8가지로 통일하고 `SupplierCallException(공급사, 원인, 원본 코드, Retry-After)` 하나로 던져요. 공통 규칙(전송 계층·HTTP 상태)은 `SupplierFailures`, B의 `resultCode` 규칙은 B 패키지 안.
- "없음"을 둘로 나눠요. `items: []`는 정상 0건, 성공 코드인데 `data`/`items` 구조가 없으면 `INVALID_RESPONSE`. 후자를 0건으로 읽으면 있는 상품이 사라지는데 지표는 성공이라 알림이 안 울려요.
- 코드·이름이 빠진 항목은 그 항목만 버리고 로그. `maxOccupancy`가 없거나 1 미만이면 객실 타입은 살리고 값만 미상(null)으로 저장해요. 기본값을 넣어 인원 필터에 추측한 값을 쓰지 않기 위해서예요. 필드 길이는 예제 범위로 두고 검사하지 않아요.
- WebClient는 공급사마다 하나를 기동 시 만들고 기본 URL·`X-Api-Key`·타임아웃을 미리 싣는다. 설정이 빠지면 기동 실패.
#### 잃는 것
공급사 원본 오류 메시지는 응답에 나가지 않아요. 인원이 미상인 객실 타입은 응답에서 인원이 비고, ② 응답에도 값이 없으면 인원 필터를 적용하지 못해요.
#### AI 검토에서 고친 것
처음 검증 규칙 초안이 "B가 `0000`이면서 데이터가 없으면 실패"였어요. 사용자가 "호출했을 때 돌려줄 데이터가 없을 수도 있다"고 지적해, 빈 목록(정상)과 구조 누락(깨진 응답)을 구분하는 규칙으로 고쳤어요. `maxOccupancy` 누락도 초안은 "그 객실 타입을 저장하지 않음"이었는데, 사용자가 "인원 값만 안 보여 주면 된다"고 바로잡아 객실 타입은 저장하고 값만 미상으로 두는 규칙(`max_occupancy` NULL 허용)으로 바꿨다. 다만 검색 응답의 최대 수용 인원은 안내 문서의 최소 계약에 들어 있어 비워 보낼 수 없으므로, ② 응답 값 → 매핑 값 순으로 채우고 둘 다 없으면 그 객실 타입을 응답에서 제외하기로 했다(사용자 지적: 문서가 담으라고 하면 담아야 해요).

### 연박 예약 가능 객실 수 판정
#### 문제
공급사는 날짜별 잔여 객실 수만 주고, N박 검색에 "몇 개를 예약할 수 있는가"는 우리가 정해야 해요.
#### 결정과 이유
기간 내 날짜별 재고의 최솟값. 하루라도 0이면 통째로 예약할 수 없어요. 처음에는 "같은 타입이면 다른 방으로 이어 잘 수 있다"를 잃는 것으로 적었는데, 응답이 객실 타입 단위라 호실 배정은 애초에 보이지 않고 최솟값이 그 경우를 잃지 않는다는 점을 확인해 정정했어요. 정말로 다루지 않는 것은 다른 객실 타입으로 대체하는 판매(업그레이드·분할 예약)이며, 각 객실 타입을 독립된 상품으로 봐요.

### 예약 불가 상품 처리
#### 문제
예약 가능 객실 수가 0인 상품을 응답에서 뺄지 0으로 노출할지.
#### 조사
소비자 화면(Booking.com·Agoda·Expedia)은 매진 숙소를 "Sold out" 문구와 함께 남겨요. 희소성 연출이 목적이고 소비자 단체 조사에서 과장이 많다고 지적됐어요. 공급사·집계 API(Expedia Rapid)는 예약 가능한 객실만 돌려줘요. 국내 앱의 표시 방식은 공개 자료를 찾지 못했어요.
#### 결정과 이유
이 API는 앱 백엔드가 쓰는 집계 API이고 프론트엔드는 비범위이므로 B2B 관행을 따라 기본 제외. 매진 노출은 화면의 마케팅 판단이라 `includeSoldOut=true` 옵션으로 넘겨요. Redis에는 재고 0도 저장해요.
#### 잃는 것
기본 응답만으로는 매진 표시를 못 하고, 제외된 개수도 없어요.

### 부분 실패 표현
#### 선택지 비교
1. 206·207 상태 코드: 대부분의 클라이언트가 오류로 처리. 폐기.
2. 전부 실패에도 빈 200: "예약 가능한 숙소가 없다"로 오해. 폐기.
3. 일부 실패 200 + `failures[{supplier, reason}]`, 전부 실패 503 + `Retry-After`, 잘못된 요청 400: 채택.
#### 결정과 이유
Reactor `Flux.merge`는 하나라도 에러면 전체를 취소하므로, 실패 변환을 공급사별 체인 안에 두어 격리한 뒤 병합해요. 검색 재시도는 사용자가 기다리므로 연결 실패·5xx만 즉시 1회, 타임아웃·429는 없어요. 크론잡의 느린 재시도(30초 × 3)와 분리해요.

### 대량 숙소 조회
#### 문제
요청당 50개 상한이라 숙소 1,000개면 묶음 20개, 5,000개면 100개. 초당 한도는 스펙에 없고 실제 공급사 공개값은 0.8\~10회.
#### 검토 과정
1. "1,000개를 ms에"는 어떤 R로도 불가능해요. 호출 하나의 지연(수백 ms)이 있고, 한도가 초당 10회여도 20묶음은 2초예요. ms는 미리 채운 캐시에서만 나와요.
2. 3초 안에 전부 채우는 데 필요한 R = 묶음 수 ÷ (3초 − 지연): 1,000개 8회, 2,000개 16회, 5,000개 40회. 2,000개부터는 공개된 어떤 한도로도 안 돼요.
3. 검색 응답 기준을 조사했어요. 3초를 넘기면 모바일 이탈 53%, 메타서치는 부분 결과를 먼저 주고 이어 붙여요(Skyscanner create/poll).
#### 결정과 이유
평소는 미리 채운 Redis라 숙소 수와 무관. Redis가 비었을 때만 응답 시간 상한 3초 안에 채운 것만 응답하고 나머지는 `pending`. R은 4회(공개 운영 한도)로 시작, 1,000개 기준 5초면 전부 복구. 예외 케이스라 받아들여요. 숙소가 늘면 갱신 잡 갱신 한 번이 늘어나며(5,000개 13분) 날짜 계층·한도 협의는 확장 설계.

### 타임아웃
연결 1초, 응답 3초를 설정값으로. 연결은 같은 리전이면 수십 ms라 1초 초과는 네트워크 문제. 응답은 사용자가 기다리는 검색이고 가장 느린 공급사가 전체 지연을 정하므로 3초에 끊어 부분 실패로 넘겨요. p95 지연 모니터로 재검토.

### Spring MVC + WebClient vs WebFlux
병렬·타임아웃·부분 실패가 필요한 곳은 공급사 호출뿐이고 MyBatis·크론잡·인메모리 조회는 블로킹이라 MVC + WebClient. 가상 스레드는 블로킹 클라이언트로 병렬을 만들 때의 대안(고루틴과 같은 M:N 모델)인데, WebClient가 논블로킹이라 필요 없어 켜지 않았어요. 검색 QPS가 매우 높아지면 `spring.threads.virtual.enabled` 검토.

### 모듈·패키지 구조

### 문서 구조
- 처음: 주제별 docs 5개에 7섹션 템플릿(개요·흐름·모델·클래스·테스트·설계 결정·엣지 케이스)을 두고, 설계 결정과 대안도 docs에 적으려 함
- 변경: 요구사항의 산출물 정의(README = 결정과 근거, docs = 아키텍처·통합 모델·API 명세, JOURNAL = 의사결정 과정)에 맞춰 docs를 3개 명세 문서로 합치고, 선택지 비교와 폐기한 대안은 JOURNAL로 옮겼어요. README 1장을 원페이저 역할로 둠
- 이유:

## 진행 기록

### Day 1 - 프로젝트 세팅, Mock Supplier, 매핑, 어댑터
#### 수행 내용
- Spring Boot 4.0.8 + Java 21(mise) + Gradle Kotlin DSL + MyBatis + MySQL 8.4(Docker) 프로젝트 생성, 개인 계정으로 커밋 설정
- 산출물 정의(README = 결론, docs = 명세, JOURNAL = 과정)에 맞춰 문서 구조 재편. 문제점을 세 묶음으로 분류하고 구현 항목과 연결, 유스케이스 정의, 테스트 전략 3층 정리
- 도메인 리서치 원페이저: 공급사 구조, 같은 객실이 여러 공급사에 있는 이유, PMS·RMS·채널 매니저, 요금·재고 변경 주기 조사
- Mock Supplier 모듈(9090): 부록 스펙대로 A·B 응답 JSON, 모드 전환(정상·장애·무응답)
- 매핑 설계 착수(정적·동적 분리, 갱신 시점, 식별자 안정성)
#### 의사결정
- 관계형 DB로 MySQL 선택 (처음 세팅은 H2): 운영 DB와 같은 엔진으로 upsert(`INSERT … ON DUPLICATE KEY UPDATE`)와 유니크 키 동작을 그대로 검증하기 위해서예요. H2는 MySQL 호환 모드가 있어도 upsert 문법이 달라 결과가 갈려요
- 로컬 실행 구성: MySQL은 Docker, 서버는 로컬 실행: DB는 사람마다 설치 버전이 달라지므로 이미지(8.4 LTS, utf8mb4, UTC)로 고정하고, 서버는 mise·Gradle Wrapper로 이미 고정되어 있어 컨테이너로 감싸면 확인 주기만 느려져요
  - 서버 Docker화(Dockerfile + compose)는 하지 않아요. 기본 설계와 로컬 실행 방법만 두고, 필요해지면 추가
- 문서 구조를 산출물 정의에 맞춰 재편 (설계 의사결정 기록 참고)
- Mock 구성은 별도 모듈: WireMock·테스트용 컨트롤러와 비교해, 사람이 직접 모드를 바꿔 가며 확인하는 용도라 독립 프로세스가 맞다고 판단. 자동 테스트는 WireMock으로 따로 가요
#### 막힌 지점과 해결 과정
- Spring Initializr 기본값(4.1.1)이 MyBatis 스타터 호환 범위 밖이라 4.0.8로 내림
- 개인 GitHub 계정 로그인이 기기 인증 승인 화면을 못 찾아 지연. 승인 후 저장소 로컬 설정으로 계정·이메일·credential helper를 개인 계정에 고정
#### 포기한 것과 이유
- 별도 PRD: 요구사항 문서가 범위를 정의하고 있어 README 1장이 그 역할을 대신
- 서버 Docker화: 위 의사결정 참고
#### 참고 자료
- 도메인 리서치 출처는 docs/domain-research.md 6장

### Day 2 - 설계 확정, 매핑·어댑터·검색 구현
#### 수행 내용
- 설계를 로컬 도면(HTML)에서 먼저 닫고 코드로 옮겼어요. 매핑(크론잡·델타·인메모리), 요금·재고 캐시(미리 채우기, 초당 4회, 5분 주기), 어댑터 경계·실패 판정 통일, 검색(연박 판정·예약 불가·부분 실패·대량 숙소)
- 구현 커밋 순서: 매핑 스키마·매퍼 → 공급사 설정 → WebClient → 숙소 목록 어댑터 A·B → 크론잡 델타·레지스트리 → sync 러너·기동/04:30 로드 → 재고·요금 어댑터 → 통합 검색 API
- Mock을 띄우고 sync 프로필로 매핑 3건을 넣은 뒤 검색을 정상·A 장애·A 무응답·전부 장애·매진 포함·잘못된 요청 순으로 확인
- 스킬 4개(run-local, query-mapping, test-search-api, mock-fault)
#### 의사결정
- 각 항목의 선택지와 이유는 위 "설계 의사결정 기록"에 있어요. 오늘 확정한 것: 캐시 어사이드 검토 후 폐기(검토 20), 허용 속도 1→8→4회(검토 22), 예약 불가 기본 제외 + includeSoldOut, 최대 인원 미상 저장·응답 필수, 어댑터는 호출과 번역까지
- 연동 지표는 코드로 넣지 않고 설계만: 안내 문서가 "설계만으로도 가능"으로 두었고 모니터는 외부 SaaS 설정이라 저장소에서 재현되지 않아요. 지표 정의·Datadog 전송 방식·알림 규칙을 architecture 4.4에 명세
- 외부 호출 실패는 전부 알림(사용자 결정): 처음 알림 규칙은 비율 기준(성공률 97% 아래 등)뿐이라 한 건짜리 실패는 429 말고 안 울렸어요. 부분 실패를 허용하는 설계일수록 실패가 응답에서 가려지므로, 공급사·DB·Redis 실패 1건 이상이면 알리고 같은 대상·원인은 5분 안에서 묶는 규칙으로 바꿨어요. 지표가 없으니 근거는 로그(Datadog 로그 모니터)
#### 막힌 지점과 해결 과정
- Testcontainers 2.x는 모듈명이 `testcontainers-mysql`, `testcontainers-junit-jupiter`로 바뀌어 `org.testcontainers:mysql`이 안 잡힘. `./gradlew dependencyManagement`로 관리 버전을 확인해 해결. 제네릭 `MySQLContainer<?>`는 deprecated라 `org.testcontainers.mysql.MySQLContainer`로 교체
- Spring Boot 4의 HTTP 클라이언트 설정 API가 `HttpClientSettings` + `ClientHttpConnectorBuilder.reactor()`로 바뀜. jar를 `javap`으로 열어 시그니처 확인
- 멀티 모듈에서 `./gradlew bootRun`이 Mock 모듈의 bootRun까지 실행해 9090 포트 충돌. 문서 명령을 `./gradlew :bootRun`으로 고침
- 8080을 다른 로컬 프로세스가 점유해 앱이 못 뜸. `--server.port=8081`로 확인하고 README에 포트 변경법 기재
- 커밋 메시지 제목과 본문 사이 빈 줄을 빠뜨려 제목이 첫 문단까지 이어짐. 두 커밋을 다시 만듦
- WireMock 최신이 4.0 beta라 안정판 3.13.2를 고정
- 금지어 hook이 README 문구 하나를 잡아 표현을 고침. hook은 로컬에만 있고 저장소에는 없어요
#### 포기한 것과 이유
- 연동 지표 코드(Micrometer 필터·Actuator): 위 의사결정 참고
- 서킷 브레이커: 선택 항목. 값(최근 10회 중 5회, 30초, 반개방 1회)만 설계
- 요금·재고 캐시(Redis)를 오늘 구현: 필수 흐름을 먼저 끝내기 위해 내일 오전 최소 구현으로 미룸. 날짜 계층 주기·핫 리스트·리더 선출은 확장 설계로만
- 검색된 조합만 채우는 캐시 어사이드: 콜드 지연 때문에 폐기(검토 20)
#### 참고 자료
- 공급사 API 한도, 검색 응답 시간, 매진 노출 관행 출처는 docs/domain-research.md 6장

### Day 3 - 요금·재고 캐시, 문서 마무리, 제출
#### 수행 내용
- 요금·재고 캐시(Redis) 최소 구현: compose에 Redis, `AvailabilityCache`(숙소당 Hash, TTL 주기 × 3, 상태 키), `AvailabilityRefreshJob`(기동 직후 + 5분마다 오늘\~+30일), 어댑터에 날짜별 조회(`fetchDailyAvailability`, A는 기간 한 번·B는 1박씩), 공급사별 호출 한도(`SupplierRateLimiter`, 초당 4회, 429면 절반), 검색의 Redis 우선 읽기와 저하 모드
- Mock의 재고·요금 응답을 요청 날짜 범위대로 예제 패턴을 반복해 생성하도록 변경. 갱신 잡의 30일 범위과 검색의 임의 날짜가 같은 값을 보게 하기 위해
- Redis·Mock·앱을 띄워 캐시 적중(7ms, fresh=false) → FLUSHALL 저하 모드 → A 장애 → fresh=true 순으로 확인. 캐시 값과 직접 호출 값이 같음(429,000 / 452,000)
- README 재구성(빠른 시작 → 구현 범위 → 흐름도 → 설계 결정 순서), 다른 문서의 장 번호 참조 정리
- 스킬 형식 통일(`Triggers`·`Do NOT use for`·`allowed-tools`, 본문은 전제 → 실행 순서 → 확인 포인트 → 주의사항)과 `query-cache`·`add-supplier` 추가
- 최종 점검, 원격 저장소 공개, 제출
#### 의사결정
- 캐시 값에 최대 인원 추가: 검색 응답의 최대 인원이 계약상 필수이고 "② 응답 값 우선" 규칙을 캐시 경로에서도 지키려면 값에 있어야 해요. 값이 약 25B에서 28B로 늘지만 listpack 조건(64B) 안이에요
- 갱신 잡을 웹 앱 안의 스케줄로 둠(팟 하나 기준). 팟이 여러 대면 잡이 중복되므로 리더 선출(또는 별도 팟)은 설계로만
- 저하 모드는 "값이 없는 숙소만 직접 호출"까지 구현하고, 응답 시간 상한 3초 초과분을 `pending`으로 표시하는 부분 응답은 설계만. 공급사별 타임아웃 3초가 응답 상한을 이미 정해요. 응답에 두었던 빈 `pending` 필드는 구현이 없는 계약을 남기지 않기 위해 뺐다
#### 막힌 지점과 해결 과정
- 테스트 클래스마다 Redis 컨테이너를 새로 띄우자 전체 실행에서만 연결 거부가 남. 컨테이너를 테스트 JVM에서 하나만 띄워 공유(싱글턴)하고 "Ready to accept connections" 로그를 기다린 뒤 ping으로 확인해 해결
- Mock이 날짜와 무관하게 9/1\~9/3 고정 응답을 줘서 갱신 잡의 범위(오늘\~+30일)과 맞지 않아 캐시가 비었어요. Mock을 날짜 범위대로 생성하도록 바꿨어요(패턴 기준일 9/1이라 예제 값은 그대로)
- 기존 검색 테스트는 캐시가 없는 상태를 가정하므로 캐시를 mock으로 비워 두고, 캐시 경로는 실제 Redis 컨테이너로 따로 검증
- 문서를 다시 읽다가 체크아웃이 캐시 범위(오늘\~+30일) 밖인 숙박은 날짜 칸 일부만 있어 숙소가 캐시 응답으로 처리되고 객실 타입은 전부 빠져 결과에서 사라지는 문제를 발견했어요. 요청 기간을 채운 객실 타입이 하나도 없으면 직접 호출로 넘기도록 고치고 테스트를 추가했어요
- 문서 점검 중 갱신 잡이 Hash에 필드를 덧쓰기만 하고 TTL을 매번 연장해 지난 날짜 필드가 영원히 쌓이는 문제를 발견. 쓸 때 MULTI로 DEL → HSET → EXPIRE 해 통째로 교체하도록 고침
#### 포기한 것과 이유
- 날짜 거리별 주기 계층, 남은 객실 ≤ 2 핫 리스트: 숙소 3개로는 의미가 없고 설정값과 문서로 충분
- 갱신 잡 첫 갱신과 readiness 연동: K8s 전용이라 로컬에서 재현되지 않아요. 첫 갱신 전 검색은 저하 모드로 응답
#### 참고 자료
- docs/architecture.md 4.5, docs/domain-research.md 6장

## 테스트 전략과 결과

### 전략
세 층으로 나눠요. 자동 테스트는 Mock Supplier 모듈(9090)에 의존하지 않고, 필요한 가짜 서버와 DB를 테스트가 직접 띄워요.

| 층 | 대상 | 도구 | 검증 시나리오 |
|---|---|---|---|
| 단위 | 정규화 변환, 연박 판정, 실패 판정 규칙, 검색 서비스의 병합·부분 실패(어댑터는 Mockito mock) | JUnit, Mockito, AssertJ | A·B 응답 → 표준 모델, 재고 0인 날이 있으면 예약 불가, A 실패 시 B 결과만 반환 |
| 어댑터 HTTP | 어댑터가 WebClient로 실제 HTTP 호출 | WireMock (테스트 안에서 실행) | 503 → 실패, 200 + E503 → 실패, 응답 지연 > 타임아웃 → 실패, 정상 → 표준 모델 |
| 통합 | 매핑 저장·재실행, 검색 전체 흐름 | @SpringBootTest + Testcontainers MySQL + WireMock | 매핑 두 번 실행해도 식별자 동일, A 장애 상태에서 검색 → B 결과 + 부분 실패 표시 |

도구 선택
- WireMock (MockWebServer 대신): URL별 규칙이라 A·B 동시 호출 중 A만 실패시키기 쉽고, 지연·장애(fault)가 내장돼 있어요. MockWebServer는 응답을 순서대로 쌓는 방식이라 병렬 호출 검증이 헷갈리기 쉬워요.
- Testcontainers (compose DB 대신): `./gradlew test`만으로 실행되고 테스트마다 깨끗한 DB를 써요. compose DB는 먼저 띄워야 하고 데이터가 테스트끼리 섞일 수 있어요.
- 의존성(wiremock-standalone, spring-boot-testcontainers, testcontainers mysql·junit-jupiter)은 테스트를 작성하는 시점에 추가해요.

### 결과

`./gradlew test` 77개 통과 (2026-09-17 기준. Docker가 떠 있어야 Testcontainers 통합 테스트가 돌아요).

| 층 | 테스트 | 개수 | 검증한 것 |
|---|---|---|---|
| 통합 (MySQL 컨테이너) | MappingMapperIntegrationTest | 4 | 같은 코드 재저장 시 id 유지, 공급사가 다르면 다른 id, 비활성 후 복귀 시 id 유지, 객실 타입 코드는 숙소 안에서만 유일 |
| 통합 (MySQL 컨테이너) | MappingSyncJobIntegrationTest | 6 | 첫 실행 삽입, 델타(변경·추가·사라짐)와 식별자 유지, 실패 공급사 건너뛰기, 재시도·비재시도, 절반 초과 사라짐 보류 |
| 단위 | MappingRegistryTest, MappingRegistryLoaderTest, MappingSyncRunnerTest | 7 | 스냅샷 조회, 기동 로드 실패 시 기동 실패, 리로드 실패 시 유지 + 5분 재시도, 종료 코드 |
| 단위 | SupplierPropertiesTest, SupplierWebClientsTest, SupplierRateLimiterTest | 7 | 설정 바인딩, 누락 설정 시 기동 실패, 호출 한도(허용 속도로 호출 간격 유지, 공급사별 독립, 429면 절반·다른 실패는 유지) |
| 어댑터 HTTP (WireMock) | SupplierAClientTest | 14 | 목록·재고 정규화(429,000 / 302,500), 날짜별 조회(기간 한 번 호출), X-Api-Key, 상태 코드 판정, Retry-After, 타임아웃, 연결 실패, 50개 분할, 묶음 부분·전부 실패, 날짜 수 불일치, 0건 vs 구조 누락 |
| 어댑터 HTTP (WireMock) | SupplierBClientTest | 11 | HTTP 200 + resultCode 실패 판정, 모르는 코드, data 누락, 0건, 최대 인원 미상, 재고·요금 정규화(452,000), 날짜별 조회(1박씩 호출) |
| 단위 | StaySearchServiceTest | 10 | 병합·내부 id 변환, 예약 불가 제외·포함, 인원 필터, 최대 인원 출처, 부분 실패·묶음 실패 표시, 전부 실패 예외, 즉시 1회 재시도(타임아웃 제외), 미매핑 코드, 요청 검증 |
| 단위 (Redis 컨테이너) | StaySearchServiceCacheTest | 6 | 전부 캐시면 공급사 호출 0, 없는 숙소만 직접 호출, 창 경계에 걸친 숙박은 직접 호출, fresh=true, 상태 키 실패 표시, Redis 장애 시 저하 모드 |
| 캐시 (Redis 컨테이너) | AvailabilityCacheTest, AvailabilityRefreshJobTest | 7 | Hash 쓰기·TTL·파이프라인 읽기, Hash 통째 교체(지난 날짜 필드가 쌓이지 않음), 값 형식, 상태 키, 갱신 잡의 창 채우기·미매핑 제외·실패 공급사 격리, 429 지수 백오프(다른 실패는 재시도 안 함) |
| 웹 슬라이스 | StaySearchControllerTest | 4 | 200 JSON 형태, 400(검증·누락·형식), 503 + Retry-After |
| 컨텍스트 | StaySupplierIntegrationApplicationTests | 1 | 로컬 MySQL로 기동 |

Mock Supplier로 직접 확인한 시나리오(README 5장 동작 확인과 같음): 캐시 적중 200(7ms, fresh=false) → FLUSHALL 뒤 저하 모드 직접 호출(같은 값) → A 장애 200 + failures UNAVAILABLE(0.2초, 재시도 포함) → A 무응답 200 + failures TIMEOUT(3.0초) → 전부 장애 503 + Retry-After → includeSoldOut으로 예약 불가 포함 → fresh=true 직접 호출 → 체크아웃 < 체크인 400. sync 프로필 실행으로 매핑 3건 저장과 종료 코드 0, 갱신 잡 첫 갱신로 숙소 3개 × 30일 채움과 TTL 15분 확인.

## AI 활용 기록

<!--
항목 형식
- 물어본 것:
- 답변 요지:
- 판단: 수용 / 수정 / 거부
- 이유:
- 관련: 문서 경로, 커밋 해시
-->

### 요구사항 분석

- 물어본 것: 요구사항 문서 전체를 필수 구현, 선택 구현, 비범위, 산출물 기준으로 요약
- 답변 요지: 필수 구현 6가지와 판단이 필요한 설계 항목 목록으로 정리
- 판단: 수용
- 이유: 3일 안에 끝내려면 필수·선택·비범위를 먼저 갈라야 일정을 배분할 수 있었어요
- 관련:

- 물어본 것: 도메인 용어(공급사, 객실 타입, net/gross, 부분 실패 등) 정리와 예시
- 답변 요지: 숙박 상품, 요금, 연동 구조, 연동 견고성 4묶음으로 용어와 시각 자료 정리
- 판단: 수정. 처음 결과가 비유 중심이라 원문 용어를 그대로 쓰도록 다시 요청했고, 용어마다 실제 서비스 화면 기준 예시를 추가하도록 요청했어요. net/gross, 연결·응답 타임아웃처럼 원문에 정의가 없는 설명은 따로 확인이 필요함
- 이유: 문서와 코드의 이름이 안내 문서 용어와 같아야 평가자가 대조하기 쉽고, 공급사 구조를 모르는 상태라 예시가 있어야 이해됐어요
- 관련:

- 물어본 것: 외부 공급사가 보통 어떤 업체이고, 두 공급사가 같은 호텔을 제공하는 경우가 어떤 경우인지 예시로 설명
- 답변 요지: 도매 업체·OTA 제휴 API·채널 매니저·호텔 체인 시스템 유형과, 호텔이 여러 판매처와 동시에 계약해 같은 객실이 여러 경로로 들어오는 구조를 예시 데이터로 설명. "싼 쪽만 보여주기"는 조건이 다른 상품을 비교하게 되므로 기본 동작으로 두지 않고 병합은 선택 항목으로 두자고 제안
- 판단: 수용. 내용을 docs/domain-research.md에 해요체로 옮겼어요. 업계 구조 설명은 일반적으로 알려진 내용이라 문서에 "특정 업체의 계약 관계를 확인한 것은 아니다"라고 표시했어요
- 이유: 같은 객실이 두 공급사에서 다른 조건(조식 등)으로 들어오는 구조를 알아야 "싼 쪽만 보여주기"를 기본 동작으로 두지 않는 결정이 서요
- 관련: docs/domain-research.md

### 저장소 구성

- 물어본 것: 평소 쓰던 다른 저장소의 에이전트용 문서 구성(CLAUDE.md, docs, 스킬) 분석과 적용 방안
- 답변 요지: CLAUDE.md(명령, 아키텍처 개요, docs 라우팅 표, 코드 규칙) + 주제별 7섹션 docs + 스킬 구성을 제안. 기간을 고려해 스킬은 4개로 줄이고, docs의 디버깅 섹션을 설계 결정 섹션으로 바꾸는 안을 제시
- 판단: 수용
- 이유: 익숙한 구성이면 문서 위치를 바로 찾고, 스킬은 기능이 생긴 뒤 만드는 편이 헛일이 없어요
- 관련: CLAUDE.md, docs/

- 물어본 것: 코드 구조를 클릭해 탐색할 수 있는 다이어그램 문서 생성(로직 지도)
- 답변 요지: 코드를 읽어 부팅·요청 파이프라인·배치·실패 스택·외부 의존·관측 존으로 나눈 SVG 지도와 노드별 상세(코드 출처·상수), 스토리 2개·시나리오 3개·표 3개를 담은 단일 HTML 을 생성. 정적 검증 스크립트와 브라우저 실측(텍스트 넘침·인터랙션)까지 수행
- 판단: 수용. 노드의 출처(파일:줄)와 상수(타임아웃·재시도·TTL)를 코드와 대조해 확인하고, GitHub 이 HTML 을 렌더하지 않아 README 에는 미리보기 이미지와 GitHub Pages 링크로 붙임
- 이유: Mermaid 흐름도는 GitHub 안에서 바로 보이고, 지도는 노드를 눌러 상수와 출처를 확인할 수 있어 둘의 역할이 달라요. 지도 엔진(CSS·JS)은 생성 도구의 템플릿이고 데이터(노드·엣지·설명)만 이 저장소 코드에서 나왔어요
- 관련: docs/system-story.html, docs/images/system-story.png

- 물어본 것: 스킬 4개의 구성을 평소 쓰던 다른 저장소의 스킬 형식과 비교해 더할 것과 뺄 것
- 답변 요지: 프런트매터에 부르는 말(Triggers)·경계(Do NOT use for)·allowed-tools를 두고 본문 절을 통일하는 형식 정리, Redis 캐시 조회(query-cache)와 신규 공급사 절차(add-supplier) 추가를 제안. commands·agents·포맷터 훅은 제외
- 판단: 수용
- 이유: 스킬이 같은 모양이어야 하나만 읽어도 나머지를 알 수 있고, 캐시 조회 수단과 신규 공급사 절차는 문서에만 있고 실행 절차가 없었어요
- 관련: .claude/skills/

- 물어본 것: AI 활용 기록 작성 형식
- 답변 요지: 일자별 진행 기록 안에 AI 활용 기록을 함께 넣는 형식 제안
- 판단: 수정. AI 활용 기록에는 날짜가 필요 없다고 판단해 단계별로 분리했어요
- 이유: 안내 문서가 보는 것은 "무엇을 물었고 어떻게 수용·수정·거부했는지"라 날짜보다 단계가 축이 돼요
- 관련: JOURNAL.md

- 물어본 것: 별도 PRD 작성이 필요한지
- 답변 요지: 요구사항 문서가 이미 범위를 정의하고 있고, 요구사항을 옮겨 적으면 원문과 겹치므로 README의 문제 요약과 구현 범위 표로 대체할 것을 제안
- 판단: 수용
- 이유: 안내 문서가 이미 범위를 정의하고, 옮겨 적으면 원문 재배포 금지와도 충돌해요
- 관련: README.md

### 프로젝트 세팅

- 물어본 것: Java 21 + MyBatis 기반 Spring Boot 프로젝트 생성
- 답변 요지: Spring Initializr 기본값(4.1.1)은 MyBatis 스타터 호환 범위 밖이라 4.0.8로 생성하고, JDK 21은 mise로 설치해 `mise.toml`에 고정했어요. 참고 자료가 많은 3.5.x와 장단점도 비교함
- 판단: 수용. 3.5.x와 비교한 뒤 4.0.8 유지
- 이유: MyBatis 스타터 호환 범위 안의 최신이고, 3.5.x보다 참고 자료는 적지만 요구사항이 4.x를 허용해 최신을 택했어요
- 관련: build.gradle.kts, mise.toml

- 물어본 것: 로컬 MySQL을 Mac에 직접 설치할지 Docker로 띄울지, 평소 쓰던 다른 저장소의 로컬 인프라 구성
- 답변 요지: 다른 저장소도 인프라는 Docker, 서버는 로컬 실행 구조임을 확인. 직접 설치는 사람마다 버전·설정이 달라지고 다른 프로젝트와 3306 포트가 충돌한다고 비교
- 판단: 수용. MySQL 8.4 LTS를 Docker(compose.yaml + my.cnf)로 띄움
- 이유: 사람마다 설치 버전·설정이 달라지는 것을 이미지로 막고, 포트 충돌은 변수로 피해요
- 관련: compose.yaml, docker/mysql/conf.d/my.cnf

- 물어본 것: 서버도 Docker로 띄우지 않는 이유
- 답변 요지: 서버는 mise, Gradle Wrapper, build.gradle.kts로 이미 버전이 고정되고, 개발 중에는 이미지 재빌드 때문에 확인 주기가 느려진다고 설명. 개발 중에는 로컬 실행, 마무리 단계에서 제출용 서버 컨테이너를 추가하는 안 제시
- 판단: 수용
- 이유: mise·Gradle Wrapper로 버전이 이미 고정되어 있고, 개발 중 이미지 재빌드는 확인 주기만 늦춰요
- 관련: docs/architecture.md

### 설계

- 물어본 것: 요금과 재고가 실제로 얼마나 자주 바뀌는지(일별·시간별·초별) 공개 자료 조사
- 답변 요지: 요금은 기본 하루 1회, 자동 RMS는 하루 12\~40회, 실시간 옵션은 예약·취소 직후. 재고는 주기가 아니라 예약 이벤트마다 바뀌고 수초 내 전파. 정적 콘텐츠는 야간·주간 배치가 관행. "초 단위로 바뀐다"던 이전 설명은 근거 없는 추론이었다고 정정
- 판단: 수용. 조사 결과를 docs/domain-research.md 4장에 정리하고, 매핑 갱신을 하루 1회 스케줄로 두는 근거로 사용. 캐시는 요청 기반 + 짧은 TTL + 예약 직전 재확인 전제로 선택 항목에 둠
- 이유: 캐시 주기와 매핑 갱신 주기를 근거 있게 정하려면 규모(시간 단위·이벤트 단위)가 필요했고, 출처 성격을 문서에 밝혀 과신을 막았어요
- 관련: docs/domain-research.md

- 물어본 것: 인스턴스가 여러 대일 때 요금·재고 캐시를 어떻게 둘지, 갱신 주기와 초당 호출 횟수의 근거
- 답변 요지: 처음에는 Redis 도입을 429 모니터 신호에 걸자고 했고, 나중에는 검색된 조합만 채우는 캐시 어사이드를 제안했어요. 허용 속도는 1회 → 8회를 제안
- 판단: 거부 후 수정. Redis는 상시 운영이고 429는 속도 조정 신호일 뿐이라고 바로잡았어요. 캐시 어사이드는 새 기간을 처음 검색한 사용자가 초 단위를 기다려 폐기하고 미리 채우기로 되돌렸어요. 8회는 예외 케이스(Redis 장애)를 위한 값이라 공개 운영 한도 4회로 시작하고 최악 5초 복구를 받아들임
- 이유: 검색 응답 시간(ms)이 기준이고, 캐시가 완전히 비는 것은 Redis 장애뿐이라 그때의 부담으로 평소 429 위험을 키울 이유가 없어요
- 관련: docs/architecture.md 4.5, 설계 의사결정 기록 "요금/재고 캐시" 검토 18·20·22

- 물어본 것: 어댑터가 응답을 가공해 DB 저장까지 해야 하는지, 서비스 계층으로 나누는 게 맞는지
- 답변 요지: 어댑터는 호출과 번역까지, 저장·병합·캐시·재시도는 서비스. 같은 어댑터를 크론잡(DB)·검색(응답)·실시간 재확인이 다르게 쓰고, 검색이 DB에 쓰면 "요금·재고 저장 금지"를 어긴다는 비교
- 판단: 수용
- 이유: 바뀌는 이유(공급사 스펙 vs 우리 저장 규칙)가 달라 한 클래스에 두면 둘 다 그 클래스를 고치게 됨
- 관련: docs/architecture.md 4.1, 설계 의사결정 기록 "어댑터 반환 형태와 실패 판정"

- 물어본 것: 예약 불가 상품을 응답에서 뺄지 0으로 노출할지, 기존 숙박 앱 관행 조사
- 답변 요지: 소비자 화면은 매진을 희소성 문구와 함께 남기고(소비자 단체가 과장이라고 지적), 공급사·집계 API는 예약 가능한 것만 돌려줘요. 국내 앱은 공개 자료가 없어요
- 판단: 수용. 집계 API 관행(기본 제외)을 따르고 매진 노출은 옵션으로
- 이유: 이 API는 앱 백엔드가 쓰는 집계 API이고 프론트엔드는 비범위
- 관련: README 4.4, docs/domain-research.md

- 물어본 것: 공급사가 최대 인원을 안 줄 때, B가 성공 코드인데 데이터가 없을 때의 처리
- 답변 요지: 처음 초안은 "그 객실 타입을 저장하지 않음", "0000 + 데이터 없음은 실패"
- 판단: 수정. 객실 타입은 저장하고 값만 미상으로 두되 응답 계약에 최대 인원이 필수라 둘 다 없으면 응답에서 제외. B는 빈 목록(정상 0건)과 구조 누락(깨진 응답)을 구분
- 이유: 값이 없다고 상품을 버리면 식별자 안정성이 깨지고, 0건을 실패로 보면 정상을 장애로, 구조 누락을 0건으로 보면 장애를 정상으로 처리하게 됨
- 관련: docs/architecture.md 4.1, docs/stay-model.md 2.1

### 구현

- 물어본 것: Testcontainers 의존성이 안 잡히는 원인
- 답변 요지: 2.x에서 모듈명이 `testcontainers-mysql`, `testcontainers-junit-jupiter`로 바뀜. `./gradlew dependencyManagement`로 관리 버전 확인
- 판단: 수용
- 이유: 공식 관리 버전 목록에서 직접 확인
- 관련: build.gradle.kts

- 물어본 것: Spring Boot 4에서 공급사별 WebClient에 연결·응답 타임아웃과 헤더를 적용하는 방법, WebClient와 어댑터의 개념과 대안
- 답변 요지: `HttpClientSettings` + `ClientHttpConnectorBuilder.reactor()`로 공급사별 커넥터를 만들고 `X-Api-Key`는 기본 헤더로. 대안(WebClient 하나 공유, 어댑터별 생성, HTTP 서비스 클라이언트, WebFlux 전면, 가상 스레드)을 비교
- 판단: 수용
- 이유: 연결 타임아웃은 커넥터 수준이라 공급사별로 다르게 주려면 어차피 커넥터가 갈려요. 가상 스레드는 블로킹 클라이언트용이라 WebClient 지정 요구와 맞지 않아요
- 관련: src/main/java/com/staysupplier/supplier/SupplierWebClients.java, README 4.7

- 물어본 것: Redis 캐시 최소 구현(갱신 잡, Hash 쓰기·읽기, 검색의 캐시 우선, 호출 한도)과 테스트
- 답변 요지: `cache/` 패키지에 저장소·갱신 잡, 어댑터에 날짜별 조회, 검색은 값이 없는 숙소만 직접 호출. Mock이 고정 날짜라 창과 안 맞는 문제를 발견해 Mock을 날짜 범위대로 생성하도록 바꿨어요. 테스트 컨테이너를 클래스마다 띄우다 연결 거부가 나서 싱글턴으로 정리
- 판단: 수용. 캐시 값에 최대 인원을 넣는 것과 pending 은 설계만 두는 것을 확인했어요
- 이유: 캐시 값과 직접 호출 값이 같은지(429,000 / 452,000)를 실제 Redis·Mock·앱으로 확인했어요
- 관련: src/main/java/com/staysupplier/cache, docs/architecture.md 4.5

- 물어본 것: 논블로킹 병렬에서 공급사 하나가 실패해도 나머지 결과가 돌아오는지, 재시도 정책과 응답 코드
- 답변 요지: `Flux.merge`는 기본이 하나라도 에러면 전체 취소라, 실패 변환을 공급사별 체인 안에 두고 병합. 재시도는 호출자별(검색 즉시 1회, 크론잡 고정 간격), 응답 코드는 일부 실패 200 + failures, 전부 503
- 판단: 수정. 크론잡 재시도는 지수 백오프 대신 고정 간격(30초 × 3회)으로. 지수 백오프는 Redis 갱신 잡의 429에만
- 이유: 크론잡은 사람이 안 기다리고 호출이 두 번뿐이라 단순한 쪽이 로그 읽기에도 나아요
- 관련: docs/architecture.md 3.2·4.3

### 테스트

- 물어본 것: Mock Supplier 모듈과 WireMock의 차이, 테스트 3층 구성
- 답변 요지: Mock 모듈은 사람이 모드를 바꿔 가며 확인하는 독립 프로세스, WireMock은 테스트가 직접 띄우는 가짜 서버. 단위·어댑터 HTTP·통합(Testcontainers) 3층 제안
- 판단: 수용
- 이유: 자동 테스트가 별도 프로세스에 의존하지 않아야 `./gradlew test`만으로 돌아요
- 관련: JOURNAL "테스트 전략과 결과"

- 물어본 것: 어댑터·크론잡·검색 테스트 작성
- 답변 요지: WireMock 스텁으로 예제 데이터 정규화·상태 코드·resultCode·타임아웃·연결 실패·50개 분할·부분 실패를, MySQL 컨테이너로 델타·재시도·보류를, 가짜 어댑터로 검색 병합·필터·실패 표현을 검증
- 판단: 수용. 묶음 부분 실패 테스트는 스텁이 중복돼 있어 정리했어요
- 이유: 기대값(429,000 / 302,500 / 452,000, 재고 최솟값 1·0)이 스펙 예제 계산과 일치
- 관련: src/test
