# JOURNAL

## 설계 의사결정 기록

<!--
결정마다 아래 형식으로 적는다. 결론만 README 4장에 옮긴다.
### 결정 이름
#### 문제
#### 선택지 비교
| 기준 | 선택지 1 | 선택지 2 |
|---|---|---|
#### 예시 데이터로 따져 보기
#### 결정과 이유
#### 폐기한 대안과 이유
#### 잃는 것
#### 다시 검토할 조건
-->

### 표준 숙박 상품 모델: 살린 정보와 버린 정보

### 요금 기준

### 재고 표현

### 정적·동적 데이터 분리
#### 문제
공급사 API는 성격이 다른 두 단계로 나뉜다. 숙소 목록(①)은 조건 없이 전체를 주고 자주 바뀌지 않는다. 재고·요금(②)은 날짜·인원을 받아야 하고 호출할 때마다 달라질 수 있다. 이 차이를 저장·호출·실패 처리에 어떻게 반영할지 정해야 한다.
#### 조사
도메인 리서치 4장. 요금은 시간 단위(자동 RMS 기준 하루 12~40회), 재고는 예약·취소 이벤트마다, 숙소 정보는 일·주 단위로 바뀐다. 업계는 정적 콘텐츠를 야간·주간 배치로 로컬 DB에 두고 동적 데이터는 검색마다 실시간 호출한다.
#### 결정과 이유
| | 정적: 숙소·객실 타입 | 동적: 재고·요금 |
|---|---|---|
| 저장 | DB 매핑 테이블 | 저장 안 함 |
| 호출 | 하루 1회 갱신 잡 + 기동 시 + 수동 | 검색 요청마다 |
| 실패 | 기존 매핑 유지, 앱은 정상 기동 | 타임아웃 뒤 부분 실패로 응답 |
| 읽기 | 앱이 기동 시 DB → 인메모리, 검색은 인메모리에서 | 어댑터 응답을 바로 사용 |
쓰는 쪽(갱신 잡)과 읽는 쪽(앱 서버)을 분리한다. 이번 범위는 단일 프로세스라 갱신 잡을 앱 안에서 `@Scheduled`로 돌리되 별도 서비스 클래스로 두고, 다중 인스턴스 환경에서는 별도 잡 프로세스(CronJob)로 떼어내고 각 인스턴스는 갱신 시각을 확인해 리로드하는 설계로 남긴다.
#### 폐기한 대안과 이유
- 검색할 때마다 숙소 목록도 호출: 자주 안 바뀌는 전체 목록을 매번 받는 낭비이고, 매핑을 저장하지 않으면 식별자 안정성을 보장할 수 없다.
- 재고·요금을 DB에 저장: 이벤트마다 바뀌는 값을 저장하면 항상 옛 값이 된다. 요구사항도 원본을 쌓아두지 말라고 한다.
#### 잃는 것
갱신 잡이 돈 뒤 다음 갱신까지 새로 추가된 숙소는 검색에 안 나온다(최대 하루). 수동 갱신으로 보완한다.

### 매핑 생성 시점과 실패 처리
#### 문제
숙소 목록 API를 언제 호출해 매핑을 만들지, 호출이 실패하면 어떻게 할지.
#### 선택지 비교
| 기준 | 앱 기동 시만 | 주기 스케줄만 | 수동 명령만 | 기동 시 + 매일 1회 + 수동 |
|---|---|---|---|---|
| 첫 검색 전 매핑 보장 | O | X (첫 실행 전 비어 있음) | X | O |
| 운영 중 변경 반영 | X (재기동 필요) | O | 사람이 해야 함 | O |
| 구현 비용 | 낮음 | 낮음 (`@Scheduled`) | 낮음 (엔드포인트) | 낮음 |
| 다중 인스턴스 | 인스턴스마다 호출 | 한 인스턴스만 실행하도록 락 필요 | - | 잡을 분리하면 해결 |
#### 결정과 이유
(처음) 기동 시 + 매일 새벽 1회 + 수동 엔드포인트로 정했다가, (변경) 갱신 잡을 K8s CronJob 별도 팟(`sync` 프로필)으로 분리하고 수동 엔드포인트는 없앴다. 웹 앱은 기동 시 DB에서 동기 로드하고 매일 04:30에 한 번 더 읽어 교체하며 공급사 숙소 목록 API를 직접 호출하지 않는다. 중간에 10분 주기 리로드를 넣었다가, 매핑이 하루 1회만 바뀌므로 인메모리도 하루 1회면 충분하다는 지적으로 뺐다. 처음 안은 숙소 정보가 일·주 단위로 바뀐다는 조사 결과에 맞춰 하루 1회로 두고, 첫 검색 전 매핑이 있어야 하므로 기동 시에도 실행하며, 급한 변경은 수동으로 반영한다. 실패하면 해당 공급사만 건너뛰고 기존 매핑을 유지한 채 기동한다. 공급사 하나 때문에 서비스 전체가 못 뜨면 안 되기 때문이다.
#### 폐기한 대안과 이유
- 기동 시만: 운영 중 숙소가 추가되면 재기동 전까지 검색에 안 나온다.
- 검색할 때마다: 정적·동적 분리 항목 참고.
- 실패 시 기동 중단: 외부 연동은 실패할 수 있다는 전제(P6)와 어긋난다.
- 웹 앱 안에서 `@Scheduled`로 갱신: 인스턴스가 여러 대면 한 인스턴스만 돌게 락이 필요하고, 웹 앱이 공급사 호출 실패의 영향을 받는다. CronJob으로 떼어내면 둘 다 사라지고 갱신 잡을 독립적으로 실행·재실행할 수 있다(동료 저장소의 runner 패턴과 같음).
- 수동 갱신 엔드포인트: 운영 API 표면이 늘고 인증 문제가 생긴다. CronJob 즉시 실행으로 대체.
- 사라진 숙소 판정을 `last_seen_at` 마커로: 컬럼을 하나 더 두고 갱신 시각을 찍는 방식을 검토했으나, 이번 목록과 DB의 active 집합을 직접 비교하는 델타 계산이 추가·사라짐·변경을 코드에 그대로 드러내고 캐시 제거와도 이어져서 이쪽을 택했다. 컬럼도 하나 줄어든다.
#### 잃는 것
새벽 갱신 사이에 생긴 변경은 최대 하루 늦게 반영된다. 목록에서 사라진 숙소는 삭제하지 않고 비활성으로만 표시하므로 테이블이 줄지 않는다.
#### 갱신 시각
"새벽"이 아니라 "소비자 접근이 가장 적은 시간대"가 기준이다. 국내 숙박 앱의 시간대별 이용률은 공개 자료가 없다. 호텔 예약 피크(저녁 9시, 예약 완료 기준·해외)와 일반 웹 트래픽 최저(03~05시, 모든 사이트 합산)는 숙소 검색을 잰 값이 아니라 가정의 근거일 뿐이므로, 한국 시간(Asia/Seoul) 03~05시 창 안에서 기본값 04:00을 설정값으로 두고, 운영에서 검색 요청 지표로 시간대별 검색량을 직접 재서 정한다. DB와 컨테이너가 UTC라 스케줄에 타임존을 명시한다. 인메모리 교체와 델타 write라 sync가 사용자 응답에 주는 영향은 거의 없고, 시간대를 고르는 이유는 공급사 호출 예산을 검색 피크와 겹치지 않게 하려는 것이다.
#### 다시 검토할 조건
공급사가 변경 알림(푸시)을 제공하면 주기 갱신을 이벤트 기반으로 바꾼다. 인스턴스가 여러 개가 되면 갱신 잡을 별도 프로세스로 분리하고 리로드 방식(주기 리로드 / 갱신 시각 확인 / pub/sub 알림)을 정한다.

### 내부 식별자 안정성
#### 문제
같은 공급사 상품이 다시 조회되면 항상 같은 내부 식별자가 나와야 한다. 재실행·동시 실행·숙소 삭제 상황에서도 지켜져야 한다.
#### 선택지 비교
| 기준 | BIGINT 자동 증가 + 유니크 키 | UUID/ULID | 공급사 코드 해시 |
|---|---|---|---|
| 같은 코드 → 같은 식별자 | 유니크 키 + upsert로 보장 | 유니크 키 + upsert로 보장 | 해시 자체로 보장 |
| DB를 새로 만들면 | 번호가 달라질 수 있음 | 값이 달라짐 | 같음 |
| 외부 노출 | 순번이 드러남 | 추측 불가 | 코드가 드러남 |
| 인덱스·조인 | 가장 가벼움 | 큼 | 중간 |
#### 결정과 이유
BIGINT 자동 증가 + `(supplier, supplier_hotel_code)` 유니크 키 + `INSERT … ON DUPLICATE KEY UPDATE`. 객실 타입은 `(hotel_id, supplier_room_type_code)` 유니크 키. 행은 절대 삭제하지 않고 목록에서 사라지면 `active=false`로 표시한다. 동시 실행은 유니크 키가 막고, upsert 한 문장이라 경합에서도 같은 행을 가리킨다.
#### 폐기한 대안과 이유
- 삭제 후 재삽입 방식 갱신: 사라졌다가 다시 나타난 숙소가 새 식별자를 받는다.
- 코드 해시: 재현성은 좋지만 충돌 처리와 길이 문제가 있고, 유니크 키만으로 같은 효과를 얻는다.
#### 잃는 것
DB를 초기화하면 식별자가 달라진다. 운영에서는 DB를 초기화하지 않는 것이 전제다.

### 요금/재고 캐시 (선택 구현, 설계만)
#### 문제
검색마다 ②를 호출하면 인스턴스 수와 검색량에 비례해 공급사 호출이 늘고 호출 한도(429)에 걸릴 수 있다. 캐시를 두면 옛 값을 보여줄 위험이 생긴다.
#### 검토 과정
1. 서버 시작 시 한 번 캐싱: 재고가 이벤트마다 바뀌어 곧 틀린다. 폐기.
2. 1시간마다 전체 폴링: ②는 날짜·인원이 필요해 전체 조합을 미리 긁을 수 없다. 스냅샷 파일을 따로 주는 공급사에서만 가능한 방식. 폐기.
3. 인스턴스별 인메모리 캐시: 인스턴스마다 값이 달라지고 호출이 인스턴스 수만큼 중복된다. 호출 한도 때문에 공유 캐시(Redis)가 필요하다고 판단.
4. "주기 갱신이면 TTL이 필요 없다": 갱신 주기가 곧 허용 오차이고, 어떤 키를 갱신 목록에 넣고 뺄지 규칙이 없으면 검색된 적 있는 모든 조합을 영원히 갱신하게 된다. 결국 만료 규칙이 필요하다.
5. 전체 미리 채우기의 갱신 주기는 호출 예산으로 정해진다: 한 바퀴 호출 수 = ⌈숙소 수/50⌉ × 날짜 창 × 인원 조합. 숙소 1,000개·365일·초당 10회면 최소 12분. 30일 창이면 1분.
6. Supplier B는 기간 총액만 주므로 날짜별 캐시로는 연박 요금을 재구성할 수 없다. 키는 날짜 범위 단위여야 한다.
7. "Redis에 몇 개씩 넣을지": 병목은 Redis 쓰기가 아니라 공급사 호출 한도다. 웜 채우기 처리량은 백그라운드 호출 예산(허용 속도 × 배분 비율)으로 정해지고, 토큰 버킷으로 고르게 호출한다. A는 긴 기간 한 번 호출로 여러 날을 채울 수 있지만 B는 검색 단위마다 호출해야 해서 비용이 크다.
8. 숙소가 수천 개로 늘면 30일 전체를 분 단위로 돌릴 수 없다. 날짜 거리별로 계층을 나눠(D-7까지 5분, D-30까지 30분~1시간, 그 너머는 콜드) 예산을 배분한다. 하루 단위 갱신은 매핑(정적)에만 해당하고, 요금·재고 웜 캐시를 하루 단위로 돌리면 시간 단위로 변하는 요금과 맞지 않는다.
9. 공급사 초당 호출 한도는 스펙에 없다(요청당 50개 상한과 429의 존재만 명시). 허용 속도를 설정값으로 두고 보수적으로 시작해 429 관측으로 자동 감속·증속(AIMD)하는 방식으로 설계한다. 산식의 "초당 10회"는 계산 예시일 뿐이다.
10. "공급사가 여러 판매처에 팔면 재고 변경이 검색보다 잦지 않은가": Look-to-Book 비율(도매 업체 집계 검색 1,400만/예약 8만 = 약 175:1, 유통사 수준 수천:1)로 보면 공급사 안에서 집계해도 재고 변경이 검색보다 두 자릿수 이상 드물다. 캐시는 인기 키에서 성립하고, 마지막 방(1→0)은 예약 직전 재확인으로 방어한다. 변동성이 높은 숙소는 불일치율로 골라 TTL을 줄이거나 우회한다.
11. 출처 검증: Look-to-Book 수치는 공식 통계가 아니다. 도매 업체 처리량은 제3자 연동 업체 페이지 인용(공식 페이지에서 미확인), 20,000:1은 2016년 경험 기반 칼럼, 상한 1,000~5,000:1은 근거 미표기 블로그. 독립된 세 출처가 같은 규모를 가리키는 점만 근거로 삼고, 문서에 출처 성격을 명시했다.
12. 캐시 주기는 변동이 잦은 항목이 정한다. 재고 변경 빈도를 공식 통계(STR 점유율 62%, SiteMinder 숙박일수 1.61박·리드타임 31일)로 역산하면 200실 호텔의 (호텔, 날짜) 키는 약 5시간, 임박 키는 약 1.6시간에 1건. 요금은 RMS 기준 30분~2시간. 둘 중 짧은 요금 쪽(30분)을 출발값 근거로 삼는다.
13. 모델 정리: 요청 시 채우고 TTL로 만료하는 방식과 주기 갱신 방식을 섞어 설명해 혼란이 있었다. 주기 갱신형 저장소로 통일한다. Redis에 표준 모델을 올려 두고 갱신 잡이 계층별 주기(D-3까지 5분, D-14까지 15분, D-30까지 60분)로 값을 갱신하며, 매일 새벽 매핑 델타를 반영한다. TTL은 만료 장치가 아니라 갱신 잡이 멈췄을 때를 위한 안전장치(주기의 3배)다.
14. 근거의 한계: RMS 갱신 횟수는 벤더 제품 사양(대리 지표)이고 재고 변경 빈도는 공식 통계 역산이다. 공급사 응답 값이 실제로 얼마나 자주 바뀌는지 잰 공개 데이터는 없다. 그래서 갱신 잡이 직전 값과 비교해 변경 감지율을 기록하고, 5% 미만이면 주기를 늘리고 30% 초과면 줄이는 규칙으로 실측 기반 조정을 설계에 넣었다.
15. "재고가 가격보다 잦지 않은가, 재고 소프트웨어의 최댓값을 재면": PMS API의 property당 초당 5회(Cloudbeds 공식) 같은 기술 상한은 실제 예약 건수보다 세 자릿수 위라 근거가 못 된다. 호텔 단위로는 재고가 잦지만(15분에 1건), 캐시 키(객실 타입·날짜) 단위로는 재고·요금 모두 시간 단위다. 짧은 쪽(요금 30분)에 허용 오차 약 15%를 적용해 임박 5분을 출발값으로 잡고 변경 감지율로 조정한다.
16. "키 집합 밖은 직접 호출"은 캐시 목적과 어긋난다는 지적을 받아 구조를 바꿨다. 인원은 스펙상 필터로만 작용하므로 adults=1로 모든 객실 타입을 받아 저장하고 검색 시 maxOccupancy로 거른다. 날짜는 Hash 필드 `{roomTypeId}:{date}`로 펼쳐 어떤 범위든 HMGET으로 조립한다. A는 30일 범위 한 번 호출로 채우고, B는 날짜마다 1박 호출로 채우되 연박 요금은 1박 합으로 근사한다(예약 직전 재확인으로 보정).
17. 최종 정리: 프런트 트래픽 절감은 우리 범위가 아니고 인스턴스별 캐시는 여러 대에서 효과가 희석되므로, 목표 구조는 관리형 Redis(예: ElastiCache) 공유 캐시로 확정. 이번 제출은 마감 일정과 필수 동작(병렬·타임아웃·부분 실패) 시연을 우선해 설계만 남기고, 도입 시점은 4.7의 429 메트릭 모니터 신호로 판단한다. 429는 초당 횟수가 아니라 HTTP 상태 코드(Too Many Requests)다.
18. 정정: Redis 도입을 429 신호에 걸지 않는다. 목표 구조에서 Redis는 상시 운영이고, 검색은 Redis만 읽으며 공급사 호출은 갱신 잡이 정해진 속도로만 낸다. 429는 그 속도를 줄이는 신호일 뿐이다. 초당 호출 횟수는 공급사당 2회에서 시작해 AIMD로 조정하고, 계층별 주기는 목표값(5/15/60분)과 예산으로 산출한 값 중 큰 쪽을 쓴다.
19. 확정: 초당 호출은 공급사당 1회에서 시작하고 상한은 4회다. 실제 공급사 API가 공개한 한도(Booking.com 샌드박스 초당 0.8회, Hotelbeds 초당 4회, Amadeus 초당 10회, Expedia는 요청 부하 기준)의 아래쪽에서 출발한다. 429는 로그·지표를 남기고 지수 백오프(최대 60초, Booking.com이 429 뒤 약 1분 차단하는 것에 맞춤) 후 절반으로 재개하며, 올리는 것은 AIMD 자동 증속 대신 Datadog 알림을 본 사람이 설정값으로 한다. 이미 모니터가 있어 자동 증속 코드는 두지 않는 편이 단순하다. 갱신 주기는 5/15/60분으로 확정. 숙소 1,000개 검산은 분당 36회 필요, 갱신 잡 예산 분당 54회(초당 1회의 90%).
#### 결정 (설계만)
- 이번 제출은 Redis를 띄우지 않는다. 검색이 Redis 자리에서 공급사 재고·요금 API를 직접 병렬 호출한다.
- 목표 구조는 상시 운영하는 관리형 Redis 공유 캐시, 주기 갱신형 저장소. 숙소당 Hash `stay:v1:{hotelId}`, 필드 `{roomTypeId}:{yyyyMMdd}`, 값은 정규화한 `재고|요금|세금|통화|조식|기준` 문자열. adults=1로 모든 객실 타입을 받아 저장하고 인원 필터는 검색 시 maxOccupancy로 한다. 검색은 HMGET으로 날짜 범위를 조립한다.
- 갱신 잡이 날짜 거리별 주기(D-3까지 5분 / D-14까지 15분 / D-30까지 60분)로 값을 갱신하고 변경 감지율을 기록해 주기를 조정한다. TTL은 안전장치(주기의 3배). 예약 직전 실시간 재확인 필수.
- 호출 정책: 공급사당 초당 1회 시작, 상한 4회, 갱신 잡 몫 90%. 429는 로그 + 지수 백오프(최대 60초) 후 절반으로 재개, 올리는 것은 사람이 설정값으로.
- 매핑 갱신 결과로 비활성 숙소 키만 삭제(델타). 표준 모델이 바뀌는 배포는 키 접두사 버전을 올려 교체.
- 원본이 아니라 정규화한 표준 모델을 캐싱한다. 정규화를 한 번만 하고 캐시 형태를 공급사와 무관하게 유지하기 위해서다.
#### 잃는 것
캐시 TTL만큼 옛 값을 보여줄 수 있다. 검색 결과를 참고값으로 두고 예약 직전 재확인으로 흡수한다.

### 어댑터 반환 형태와 실패 판정
#### 문제
A는 HTTP 상태 코드로, B는 HTTP 200 + `resultCode`로 실패를 알린다. 응답 구조도 다르다(`items[].hotelCode` / `data.items[].propertyId`). 이 차이가 크론잡·검색 코드로 새어 나가지 않게 하면서, 신규 공급사를 붙일 때 고치는 범위를 좁혀야 한다.
#### 선택지 비교
1. 서비스에서 WebClient를 직접 부르고 JSON을 Map으로 읽기: 가장 빠르지만 A·B 분기가 서비스에 퍼지고 공급사 C가 오면 서비스를 고친다. 폐기.
2. A·B 필드를 모두 담은 공통 DTO 하나: 절반이 항상 null이고 어느 필드가 어느 공급사 것인지 코드가 말해 주지 않는다. 폐기.
3. `switch (supplier)` 분기: 새 공급사마다 switch를 고쳐야 한다. 폐기.
4. 인터페이스 하나 + 공급사별 패키지 구현 + 전용 형식은 package-private: 채택.
5. 실패를 결과 객체(성공/실패)로 반환 vs 예외 하나로 통일: 타임아웃·5xx는 WebClient가 예외로 주므로 어댑터마다 예외→결과 변환을 반복하게 된다. 어댑터는 예외 하나로 통일하고 호출자가 공급사별 결과로 감싸는 쪽을 택했다. 묶음 여러 개 중 일부만 실패하는 ② 호출에서는 부분 결과가 필요하므로, ② 설계에서 `SupplierFetchResult(성공 항목, 실패 묶음)`을 추가하기로 했다.
6. 어댑터가 저장까지 할지: 같은 어댑터를 크론잡(DB 델타)·검색(응답·캐시)·실시간 재확인이 다르게 쓰고, 검색이 DB에 쓰면 "요금·재고 저장 금지"를 어긴다. 호출과 번역까지만 어댑터, 저장은 서비스. 재시도도 호출자마다 정책이 달라(크론잡 30초×3, 검색 200ms×1) 호출자 책임.
#### 결정과 이유
- `SupplierClient` 인터페이스, `supplier.a`/`supplier.b` 구현체, 전용 응답 형식은 package-private. 밖으로 나가는 것은 `SupplierHotel`·`SupplierRoomType`·`FailureReason`뿐.
- 실패는 `FailureReason` 8가지로 통일하고 `SupplierCallException(공급사, 원인, 원본 코드, Retry-After)` 하나로 던진다. 공통 규칙(전송 계층·HTTP 상태)은 `SupplierFailures`, B의 `resultCode` 규칙은 B 패키지 안.
- "없음"을 둘로 나눈다. `items: []`는 정상 0건, 성공 코드인데 `data`/`items` 구조가 없으면 `INVALID_RESPONSE`. 후자를 0건으로 읽으면 있는 상품이 사라지는데 지표는 성공이라 알림이 안 울린다.
- 코드·이름이 빠진 항목은 그 항목만 버리고 로그. `maxOccupancy`가 없거나 1 미만이면 객실 타입은 살리고 값만 미상(null)으로 저장한다. 기본값을 넣어 인원 필터에 추측한 값을 쓰지 않기 위해서다. 필드 길이는 예제 범위로 두고 검사하지 않는다.
- WebClient는 공급사마다 하나를 기동 시 만들고 기본 URL·`X-Api-Key`·타임아웃을 미리 싣는다. 설정이 빠지면 기동 실패.
#### 잃는 것
공급사 원본 오류 메시지는 응답에 나가지 않는다. 인원이 미상인 객실 타입은 응답에서 인원이 비고, ② 응답에도 값이 없으면 인원 필터를 적용하지 못한다.
#### AI 검토에서 고친 것
처음 검증 규칙 초안이 "B가 `0000`이면서 데이터가 없으면 실패"였다. 사용자가 "호출했을 때 돌려줄 데이터가 없을 수도 있다"고 지적해, 빈 목록(정상)과 구조 누락(깨진 응답)을 구분하는 규칙으로 고쳤다. `maxOccupancy` 누락도 초안은 "그 객실 타입을 저장하지 않음"이었는데, 사용자가 "인원 값만 안 보여 주면 된다"고 바로잡아 객실 타입은 저장하고 값만 미상으로 두는 규칙(`max_occupancy` NULL 허용)으로 바꿨다.

### 연박 예약 가능 객실 수 판정

### 예약 불가 상품 처리

### 부분 실패 표현

### 대량 숙소 조회

### 타임아웃

### Spring MVC + WebClient vs WebFlux

### 모듈·패키지 구조

### 문서 구조
- 처음: 주제별 docs 5개에 7섹션 템플릿(개요·흐름·모델·클래스·테스트·설계 결정·엣지 케이스)을 두고, 설계 결정과 대안도 docs에 적으려 함
- 변경: 요구사항의 산출물 정의(README = 결정과 근거, docs = 아키텍처·통합 모델·API 명세, JOURNAL = 의사결정 과정)에 맞춰 docs를 3개 명세 문서로 합치고, 선택지 비교와 폐기한 대안은 JOURNAL로 옮김. README 1장을 원페이저 역할로 둠
- 이유:

## 진행 기록

### Day 1 - 프로젝트 세팅, Mock Supplier, 매핑, 어댑터
#### 수행 내용
-
#### 의사결정
<!-- [선택한 것]: 이유 / 검토했다가 버린 대안과 그 이유 -->
- 관계형 DB로 MySQL 선택 (처음 세팅은 H2): 이유:
- 로컬 실행 구성: MySQL은 Docker, 서버는 로컬 실행: 이유:
  - 서버 Docker화(Dockerfile + compose)는 하지 않음. 기본 설계와 로컬 실행 방법만 두고, 필요해지면 추가
- 문서 구조를 산출물 정의에 맞춰 재편 (설계 의사결정 기록 참고)
#### 막힌 지점과 해결 과정
-
#### 포기한 것과 이유
-
#### 참고 자료
-

### Day 2 - 통합 검색 API, 연동 견고성, 연동 지표
#### 수행 내용
-
#### 의사결정
<!-- [선택한 것]: 이유 / 검토했다가 버린 대안과 그 이유 -->
-
#### 막힌 지점과 해결 과정
-
#### 포기한 것과 이유
-
#### 참고 자료
-

### Day 3 - 문서, 마무리
#### 수행 내용
-
#### 의사결정
<!-- [선택한 것]: 이유 / 검토했다가 버린 대안과 그 이유 -->
-
#### 막힌 지점과 해결 과정
-
#### 포기한 것과 이유
-
#### 참고 자료
-

## 테스트 전략과 결과

### 전략
세 층으로 나눈다. 자동 테스트는 Mock Supplier 모듈(9090)에 의존하지 않고, 필요한 가짜 서버와 DB를 테스트가 직접 띄운다.

| 층 | 대상 | 도구 | 검증 시나리오 |
|---|---|---|---|
| 단위 | 정규화 변환, 연박 판정, 실패 판정 규칙, 검색 서비스의 병합·부분 실패(어댑터는 Mockito mock) | JUnit, Mockito, AssertJ | A·B 응답 → 표준 모델, 재고 0인 날이 있으면 예약 불가, A 실패 시 B 결과만 반환 |
| 어댑터 HTTP | 어댑터가 WebClient로 실제 HTTP 호출 | WireMock (테스트 안에서 실행) | 503 → 실패, 200 + E503 → 실패, 응답 지연 > 타임아웃 → 실패, 정상 → 표준 모델 |
| 통합 | 매핑 저장·재실행, 검색 전체 흐름 | @SpringBootTest + Testcontainers MySQL + WireMock | 매핑 두 번 실행해도 식별자 동일, A 장애 상태에서 검색 → B 결과 + 부분 실패 표시 |

도구 선택
- WireMock (MockWebServer 대신): URL별 규칙이라 A·B 동시 호출 중 A만 실패시키기 쉽고, 지연·장애(fault)가 내장돼 있다. MockWebServer는 응답을 순서대로 쌓는 방식이라 병렬 호출 검증이 헷갈리기 쉽다.
- Testcontainers (compose DB 대신): `./gradlew test`만으로 실행되고 테스트마다 깨끗한 DB를 쓴다. compose DB는 먼저 띄워야 하고 데이터가 테스트끼리 섞일 수 있다.
- 의존성(wiremock-standalone, spring-boot-testcontainers, testcontainers mysql·junit-jupiter)은 테스트를 작성하는 시점에 추가한다.

### 결과
<!-- 실행 명령, 통과 여부, 검증한 시나리오(정상 / 공급사 장애 / 무응답 등) -->

## AI 활용 기록

<!--
항목 형식
- 물어본 것:
- 답변 요지:
- 판단: 수용 / 수정 / 거부
- 이유:
- 관련: 문서 경로, 커밋 해시
-->

### 요구사항 분석

- 물어본 것: 요구사항 문서 전체를 필수 구현, 선택 구현, 비범위, 산출물 기준으로 요약
- 답변 요지: 필수 구현 6가지와 판단이 필요한 설계 항목 목록으로 정리
- 판단: 수용
- 이유:
- 관련:

- 물어본 것: 도메인 용어(공급사, 객실 타입, net/gross, 부분 실패 등) 정리와 예시
- 답변 요지: 숙박 상품, 요금, 연동 구조, 연동 견고성 4묶음으로 용어와 시각 자료 정리
- 판단: 수정. 처음 결과가 비유 중심이라 원문 용어를 그대로 쓰도록 다시 요청했고, 용어마다 실제 서비스 화면 기준 예시를 추가하도록 요청함. net/gross, 연결·응답 타임아웃처럼 원문에 정의가 없는 설명은 따로 확인이 필요함
- 이유:
- 관련:

- 물어본 것: 외부 공급사가 보통 어떤 업체이고, 두 공급사가 같은 호텔을 제공하는 경우가 어떤 경우인지 예시로 설명
- 답변 요지: 도매 업체·OTA 제휴 API·채널 매니저·호텔 체인 시스템 유형과, 호텔이 여러 판매처와 동시에 계약해 같은 객실이 여러 경로로 들어오는 구조를 예시 데이터로 설명. "싼 쪽만 보여주기"는 조건이 다른 상품을 비교하게 되므로 기본 동작으로 두지 않고 병합은 선택 항목으로 두자고 제안
- 판단: 수용. 내용을 docs/domain-research.md에 해요체로 옮김. 업계 구조 설명은 일반적으로 알려진 내용이라 문서에 "특정 업체의 계약 관계를 확인한 것은 아니다"라고 표시함
- 이유:
- 관련: docs/domain-research.md

### 저장소 구성

- 물어본 것: 평소 쓰던 다른 저장소의 에이전트용 문서 구성(CLAUDE.md, docs, 스킬) 분석과 적용 방안
- 답변 요지: CLAUDE.md(명령, 아키텍처 개요, docs 라우팅 표, 코드 규칙) + 주제별 7섹션 docs + 스킬 구성을 제안. 기간을 고려해 스킬은 4개로 줄이고, docs의 디버깅 섹션을 설계 결정 섹션으로 바꾸는 안을 제시
- 판단: 수용
- 이유:
- 관련: CLAUDE.md, docs/

- 물어본 것: AI 활용 기록 작성 형식
- 답변 요지: 일자별 진행 기록 안에 AI 활용 기록을 함께 넣는 형식 제안
- 판단: 수정. AI 활용 기록에는 날짜가 필요 없다고 판단해 단계별로 분리함
- 이유:
- 관련: JOURNAL.md

- 물어본 것: 별도 PRD 작성이 필요한지
- 답변 요지: 요구사항 문서가 이미 범위를 정의하고 있고, 요구사항을 옮겨 적으면 원문과 겹치므로 README의 문제 요약과 구현 범위 표로 대체할 것을 제안
- 판단: 수용
- 이유:
- 관련: README.md

### 프로젝트 세팅

- 물어본 것: Java 21 + MyBatis 기반 Spring Boot 프로젝트 생성
- 답변 요지: Spring Initializr 기본값(4.1.1)은 MyBatis 스타터 호환 범위 밖이라 4.0.8로 생성하고, JDK 21은 mise로 설치해 `mise.toml`에 고정. 참고 자료가 많은 3.5.x와 장단점도 비교함
- 판단: 수용. 3.5.x와 비교한 뒤 4.0.8 유지
- 이유:
- 관련: build.gradle.kts, mise.toml

- 물어본 것: 로컬 MySQL을 Mac에 직접 설치할지 Docker로 띄울지, 평소 쓰던 다른 저장소의 로컬 인프라 구성
- 답변 요지: 다른 저장소도 인프라는 Docker, 서버는 로컬 실행 구조임을 확인. 직접 설치는 사람마다 버전·설정이 달라지고 다른 프로젝트와 3306 포트가 충돌한다고 비교
- 판단: 수용. MySQL 8.4 LTS를 Docker(compose.yaml + my.cnf)로 띄움
- 이유:
- 관련: compose.yaml, docker/mysql/conf.d/my.cnf

- 물어본 것: 서버도 Docker로 띄우지 않는 이유
- 답변 요지: 서버는 mise, Gradle Wrapper, build.gradle.kts로 이미 버전이 고정되고, 개발 중에는 이미지 재빌드 때문에 확인 주기가 느려진다고 설명. 개발 중에는 로컬 실행, 마무리 단계에서 제출용 서버 컨테이너를 추가하는 안 제시
- 판단: 수용
- 이유:
- 관련: docs/architecture.md

### 설계

- 물어본 것: 요금과 재고가 실제로 얼마나 자주 바뀌는지(일별·시간별·초별) 공개 자료 조사
- 답변 요지: 요금은 기본 하루 1회, 자동 RMS는 하루 12~40회, 실시간 옵션은 예약·취소 직후. 재고는 주기가 아니라 예약 이벤트마다 바뀌고 수초 내 전파. 정적 콘텐츠는 야간·주간 배치가 관행. "초 단위로 바뀐다"던 이전 설명은 근거 없는 추론이었다고 정정
- 판단: 수용. 조사 결과를 docs/domain-research.md 4장에 정리하고, 매핑 갱신을 하루 1회 스케줄로 두는 근거로 사용. 캐시는 요청 기반 + 짧은 TTL + 예약 직전 재확인 전제로 선택 항목에 둠
- 이유:
- 관련: docs/domain-research.md

### 구현

### 테스트
